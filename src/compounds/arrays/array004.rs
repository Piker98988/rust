//! Arrays; Book chapter 6.2.4; Code convention 004::02::04

/*

fn main() {
    // Fix the error
    let _arr = [1, 2, '3'];

    println!("Success!");
}
*/


pub fn main() {
    // Fix the error
    let _arr = [1, 2, 3];
    // ? obviously the problem is that the 3 is a &str instead of an i32
    
    println!("Success!");
}